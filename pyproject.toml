[project]
name = "fsm-demo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "python-statemachine>=3.1.2",
    "requests>=2.34.2",
]
